services:
  dotfiles-test:
    build: .
    tty: true
    stdin_open: true
    volumes:
      # Source is inspected only; installer experiments copy it inside the disposable container.
      - ..:/workspace:ro
    working_dir: /workspace
    environment:
      - DEBIAN_FRONTEND=noninteractive
    command: /bin/bash
