# Generated by `herdr integration install pi`; machine state, not configuration.
# Kept here rather than in extensions/.gitignore: Pi's resource discovery honours
# .gitignore files inside the directories it scans, so an ignore file in extensions/
# would hide the herdr integration from Pi itself (verified on Pi 0.84.4).
extensions/herdr-agent-state.ts
